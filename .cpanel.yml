---
deployment:
  tasks:
    # App is served directly from this repo's /public (see DEPLOY.md), so no
    # file copying is needed — just make sure Laravel's writable dirs are writable.
    - /bin/chmod -R 775 /home/manifold/repositories/manifold-timer/storage /home/manifold/repositories/manifold-timer/bootstrap/cache
