---
deployment:
  tasks:
    # Deploy tasks run from the repository root, so use relative paths (works
    # regardless of the repo folder name). App is served from this repo's
    # /public, so no file copying is needed — just ensure Laravel's writable
    # dirs are writable.
    - /bin/chmod -R 775 storage bootstrap/cache
