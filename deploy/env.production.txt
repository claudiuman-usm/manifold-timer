# =====================================================================
# Manifold Timer — PRODUCTION .env
# Copy this file's contents to  /home/manifold/repositories/manifold-timer/.env
# (create it with cPanel File Manager). Fill the <PLACEHOLDERS>. Never commit .env.
# =====================================================================

APP_NAME="Manifold Timer"
APP_ENV=production
APP_KEY=<PASTE_KEY_HERE>          # generate locally: php artisan key:generate --show
APP_DEBUG=false
APP_URL=https://timer.manifold.ro

APP_LOCALE=en
APP_TIMEZONE=Europe/Bucharest     # server time drives the whole app — set this

# --- Parent PIN (CHANGE from the default!) ---
PARENT_PIN=<CHOOSE_A_PARENT_PIN>

# --- Database (create the DB + user in cPanel > MySQL Databases) ---
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=<manifold_timer_db>
DB_USERNAME=<manifold_dbuser>
DB_PASSWORD=<the_db_password_you_set>

# --- File-based session/cache/queue: NO extra DB tables needed ---
SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_SECURE_COOKIE=true         # you'll have HTTPS
CACHE_STORE=file
QUEUE_CONNECTION=sync

# --- Logging / mail (defaults are fine for this app) ---
LOG_CHANNEL=stack
LOG_LEVEL=error
MAIL_MAILER=log
